name: Test Release
runs:
  using: composite
  steps:
    - name: Install and Start Vault
      uses: ./.github/actions/setup-vault
    - name: Test Release
      shell: bash
      run:  ./mvnw --settings .mvn/release-settings.xml --no-transfer-progress --batch-mode clean verify
